#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_jsmin@1.0.1@jsmin/bin/jsmin" "$@"
  ret=$?
else 
  node  "$basedir/../../../_jsmin@1.0.1@jsmin/bin/jsmin" "$@"
  ret=$?
fi
exit $ret
